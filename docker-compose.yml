services:
  app:
    build: .
    container_name: netsight-central-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    env_file:
      - .env
    volumes:
      - /var/run/postgresql:/var/run/postgresql
    deploy:
      resources:
        limits:
          memory: 256M
